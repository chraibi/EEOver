language: cpp
compiler:
- g++

branches:
  only:
  - master

before_script:
  - sudo apt-get update -qq
  - sudo apt-get install libboost-all-dev

notifications:
   email:
     recipients:
       - mohcine@chraibi.de
     on_success: change
     on_failure: always

  
script:
  - pwd
  - make        
  - ./overlap testcases.txt 1
